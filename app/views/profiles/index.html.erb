<h1>Search result</h1>

<%= render "search_form" %>

<br>

<% if @profiles.any? %>
  <%= render "friends_search_results", profile_to_match: current_user_profile, profiles: @profiles %>
<% else %>
  <p>Sorry, no users found.</p>
<% end %>
